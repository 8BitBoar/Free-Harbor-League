invite_chosen_one_to_court = {
    on_own_nation = yes
    message = no
    context_menu_click_mode = click
    ai_tick = never
    
    potential = {
        scope:actor = {
            tag = FHL
        }    
    }
    
    allow = {
    }
    
    effect = {
        scope:actor = {
            country_event = { id = fhl_immortal_events.1 }
        }
    }
    
}