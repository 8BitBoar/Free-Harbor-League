invite_chosen_one_to_court = {
    on_own_nation = yes
    message = no
    ai_tick = never
    
    potential = {
    }
    
    allow = {
    }
    
    select_trigger = {
        looking_for_a =  character
        source = actor
        target_flag = reciepient
        name = "choose_character"
        column = {
            data = name
        }
        visible = {
            is_alive = yes
            is_adult = yes
        }
        
    }

    effect = {
        scope:actor = {
            custom_tooltip = "NOBLE_FEMALE"
            hidden_effect = {
                fhl_create_female_character_effect = yes
            }
        }
    }

}
